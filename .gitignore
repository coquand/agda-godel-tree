*.agdai
*.aux
*.log
*.out
*~

# Only BRA/ directory is tracked going forward.
# Guard/ is frozen history: still present on disk and in git history
# up through commit d2ababb, but no longer tracked.  Do not consult
# Guard/*.agda for the BRA/ rebuild — work from guard15 directly.
/*
!/BRA/
!/.gitignore
